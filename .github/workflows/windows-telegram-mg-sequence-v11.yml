name: Windows Telegram MG Sequence V11

on:
  push:
    branches:
      - formula/current-clean-research-v1
    paths:
      - telegram-mg-sequence-v11-requests/current.json

permissions:
  contents: read

concurrency:
  group: a1-windows-telegram-mg-sequence-v11
  cancel-in-progress: true

jobs:
  study:
    name: Run governed 10-3-4 sequence-aware MG research
    runs-on: [self-hosted, windows, x64, a1-clean-parity]
    timeout-minutes: 720
    steps:
      - name: Checkout exact research revision
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Validate request
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          $req = Get-Content -LiteralPath 'telegram-mg-sequence-v11-requests/current.json' -Raw -Encoding UTF8 | ConvertFrom-Json
          if ($req.schema -ne 'A1_TELEGRAM_MG_SEQUENCE_V11_REQUEST_V1') { throw 'Unsupported V11 request schema.' }
          if ($req.enabled -ne $true) { throw 'V11 request not enabled.' }
          $json = $req | ConvertTo-Json -Compress -Depth 20
          "A1_MG_V11_REQUEST_JSON=$json" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Verify Drive credential bindings
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($path in @($env:A1_DRIVE_READER_CREDENTIALS, $env:A1_DRIVE_WRITER_CREDENTIALS)) {
            if ([string]::IsNullOrWhiteSpace($path) -or -not (Test-Path -LiteralPath $path -PathType Leaf)) { throw 'Drive credential binding missing.' }
          }

      - name: Install compile and test
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          py -3.11 -m pip install -e '.[test]'
          if ($LASTEXITCODE -ne 0) { throw 'MG_V11_INSTALL_FAILED' }
          py -3.11 -m py_compile src/a1clean/formula_research/telegram_mg_sequence_v11.py
          if ($LASTEXITCODE -ne 0) { throw 'MG_V11_COMPILE_FAILED' }
          py -3.11 -m pytest -q tests/test_telegram_mg_sequence_v11.py
          if ($LASTEXITCODE -ne 0) { throw 'MG_V11_TESTS_FAILED' }

      - name: Run V11 sequence-aware full-corpus research
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          $req = $env:A1_MG_V11_REQUEST_JSON | ConvertFrom-Json
          & py -3.11 -m a1clean.formula_research.telegram_mg_sequence_v11 `
            --output 'telegram-mg-sequence-v11-result.json' `
            --prior-days ([string][int]$req.prior_days) `
            --buy-fee-pct ([string][double]$req.execution.buy_fee_pct) `
            --sell-fee-pct ([string][double]$req.execution.sell_fee_pct) `
            --min-support ([string][int]$req.min_support)
          if ($LASTEXITCODE -ne 0) { throw "MG_V11_RUNTIME_FAILED:$LASTEXITCODE" }

      - name: Upload V11 artifact
        uses: actions/upload-artifact@v4
        with:
          name: telegram-mg-sequence-v11-${{ github.run_id }}
          path: telegram-mg-sequence-v11-result.json
          if-no-files-found: error
          retention-days: 30
