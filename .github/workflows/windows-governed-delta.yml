name: Windows Governed Delta Machine

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a1-windows-governed-delta-machine
  cancel-in-progress: false

jobs:
  governed-delta:
    name: Permanent governed delta machine - shadow commit policy
    runs-on: [self-hosted, windows, x64, a1-clean-parity]
    timeout-minutes: 240
    env:
      A1_RAW_DIR: 'D:\TRADING SYSTEM\02_CURRENT_HISTORICAL_RAW_DATA_UJI'
    steps:
      - name: Require migration parity branch
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          if ('${{ github.ref_name }}' -ne 'migration/parity-v2') {
            throw 'This workflow must be dispatched on migration/parity-v2.'
          }

      - name: Checkout production-machine code
        uses: actions/checkout@v4

      - name: Verify separate external Drive credential bindings exist
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          $bindings = @(
            @{ Name = 'A1_DRIVE_READER_CREDENTIALS'; Path = $env:A1_DRIVE_READER_CREDENTIALS },
            @{ Name = 'A1_DRIVE_WRITER_CREDENTIALS'; Path = $env:A1_DRIVE_WRITER_CREDENTIALS }
          )
          foreach ($binding in $bindings) {
            if ([string]::IsNullOrWhiteSpace($binding.Path)) {
              throw "$($binding.Name) is not configured for the runner process."
            }
            if (-not (Test-Path -LiteralPath $binding.Path -PathType Leaf)) {
              throw "$($binding.Name) points to a credential file that does not exist. Credential content/path is intentionally not printed."
            }
          }
          Write-Host 'Drive reader credential binding: PRESENT'
          Write-Host 'Drive writer credential binding: PRESENT'

      - name: Install governed machine package
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          py -3.11 -m pip install -e .

      - name: Re-verify governed Drive access separation
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          py -3.11 -m a1clean.cli drive-preflight

      - name: Run permanent governed delta machine in shadow commit mode
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          py -3.11 -m a1clean.cli governed-delta --mode SHADOW

      - name: Safety declaration
        if: always()
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          Write-Host 'PERMANENT GOVERNED DELTA MACHINE.'
          Write-Host 'Gate F runs the same production delta-state engine under SHADOW commit policy; this is not a separate tester engine.'
          Write-Host 'Canonical RAW controls dynamic source membership and no source count is hard-coded.'
          Write-Host 'VERIFIED_UNCHANGED sources are reused; only NEW/CHANGED/replacement sources enter frozen V2 source processing.'
          Write-Host 'Checkpoints, processed delta artifacts, control bundle, and canonical deployment plan persist only under PARITY_STAGING while Gate F is open.'
          Write-Host 'Canonical RAW and governed CURRENT are not written by this workflow.'
          Write-Host 'No sampling, filtering, behavior-label creation, formula, signal, threshold, selector, optimization, or Telegram analytical logic is introduced.'
