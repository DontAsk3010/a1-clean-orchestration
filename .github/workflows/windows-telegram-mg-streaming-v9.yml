name: Windows Telegram MG Streaming Multi Logic V9

on:
  push:
    branches:
      - formula/current-clean-research-v1
    paths:
      - telegram-mg-streaming-v9-requests/current.json

permissions:
  contents: read

concurrency:
  group: a1-windows-telegram-mg-multihypothesis-v8
  cancel-in-progress: true

jobs:
  study:
    name: Stream full corpus across 117 MG precursor ignition formulas
    runs-on: [self-hosted, windows, x64, a1-clean-parity]
    timeout-minutes: 720
    steps:
      - name: Checkout exact research revision
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Validate request
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          $req = Get-Content -LiteralPath 'telegram-mg-streaming-v9-requests/current.json' -Raw -Encoding UTF8 | ConvertFrom-Json
          if ($req.schema -ne 'A1_TELEGRAM_MG_STREAMING_V9_REQUEST_V1') { throw 'Unsupported V9 request schema.' }
          if ($req.enabled -ne $true) { throw 'V9 request not enabled.' }
          $json = $req | ConvertTo-Json -Compress -Depth 20
          "A1_MG_V9_REQUEST_JSON=$json" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Verify Drive credential bindings
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($path in @($env:A1_DRIVE_READER_CREDENTIALS, $env:A1_DRIVE_WRITER_CREDENTIALS)) {
            if ([string]::IsNullOrWhiteSpace($path) -or -not (Test-Path -LiteralPath $path -PathType Leaf)) { throw 'Drive credential binding missing.' }
          }

      - name: Install compile test
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          py -3.11 -m pip install -e '.[test]'
          if ($LASTEXITCODE -ne 0) { throw 'MG_V9_INSTALL_FAILED' }
          py -3.11 -m py_compile src/a1clean/formula_research/telegram_mg_streaming_multilogic_v9.py
          if ($LASTEXITCODE -ne 0) { throw 'MG_V9_COMPILE_FAILED' }
          py -3.11 -m pytest -q tests/test_telegram_mg_streaming_multilogic_v9.py
          if ($LASTEXITCODE -ne 0) { throw 'MG_V9_TESTS_FAILED' }

      - name: Run V9 streaming multilogic discovery
        shell: 'powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "{0}"'
        run: |
          $ErrorActionPreference = 'Stop'
          $req = $env:A1_MG_V9_REQUEST_JSON | ConvertFrom-Json
          & py -3.11 -m a1clean.formula_research.telegram_mg_streaming_multilogic_v9 `
            --output 'telegram-mg-streaming-v9-result.json' `
            --prior-days ([string][int]$req.prior_days) `
            --buy-fee-pct ([string][double]$req.execution.buy_fee_pct) `
            --sell-fee-pct ([string][double]$req.execution.sell_fee_pct) `
            --min-support ([string][int]$req.min_support) `
            --min-unique-days ([string][int]$req.min_unique_days)
          if ($LASTEXITCODE -ne 0) { throw "MG_V9_RUNTIME_FAILED:$LASTEXITCODE" }

      - name: Upload V9 artifact
        uses: actions/upload-artifact@v4
        with:
          name: telegram-mg-streaming-v9-${{ github.run_id }}
          path: telegram-mg-streaming-v9-result.json
          if-no-files-found: error
          retention-days: 30
